{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "booth-module-store.serviceAccountName" . }}
  labels:
    {{- include "booth-module-store.labels" . | nindent 4 }}
{{- end }}

# booth-module-store

Not started yet. This repo is the Module Store: the catalog of installable Project Booth modules (bundled + optional external registries) and the browsing/install UI, calling `booth-core`'s install API. Mandatory module — see `booth-architecture`'s ADR 0027.

Before writing any code here, a fresh Claude Code session should be given the kickoff prompt built from `../booth-architecture/agent-briefs/module-store.md`.
